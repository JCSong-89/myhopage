import { Router } from "express";

const router = Router();
/*
router.post("/:boardId", saveComment);
router.post("/:boardId:/commentId", deleteComment); // 삭제
router.put("/:board/:commentId", updateComment);
*/
export default router;
